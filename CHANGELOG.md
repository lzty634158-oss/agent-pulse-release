# AgentPulse 版本更新记录

## 0.4.6 - 2026-09-03

### 桌面端：Webhook 多平台推送

- 新增 Webhook 多平台推送：支持企业微信 / 飞书 / 钉钉 / Telegram / Slack / 自定义 Webhook。

### 桌面端：修复

- 修复配置页“模拟一次真实事件”调用 `/api/webhook/simulate` 返回 404 的问题（config-ui 未代理该接口到 daemon，已补齐）。

### 固件

- 升级 ESP32-C3 灯具固件至 `0.1.24-25`，与桌面端 0.4.6 一同发布。

## 0.4.5 - 2026-08-29

### 桌面端：状态显示修复

1. 修复实时事件状态被灯光颜色污染：Hook 事件的语义状态（绿/黄/红）不再读取各 Agent 的灯光配色，改由事件语义决定，避免灯光方案把运行中的事件误显示为绿色。Claude Code 等 Agent 的 `pre-tool-use` / `post-tool-use` 现正确显示为黄色（运行中）。
2. 修复 CodeBuddy 等 Agent 的 `post-tool-use` 被误判为红色：不再把工具返回的正常 `message` 当作错误信号，仅 `payload.error` / `tool_response.error` / `stderr` / 中断 才计为错误，修复 VS Code 扩展类 Agent 的状态错乱。
3. 实时事件面板与状态栏状态对各 Agent（Claude Code / CodeBuddy / WorkBuddy / Codex / Cursor / Copilot）一致生效，配置误配不再影响面板状态。
4. 修复实体灯 LED 颜色不跟随最新状态（回归自本版 0.4.5 状态显示修复）：USB / BLE / 局域网三条推送路径原来都优先读取灯光方案里 event 的固定配色，导致无论 Hook 推的语义状态怎么变，灯永远显示该 event 的固定色，与面板脱节。现改为灯颜色与闪烁模式**优先跟随语义 AgentStatus**（与状态面板同一来源），灯光方案的 event 配色仅作为无语义色时的兜底。CodeBuddy + BLE 场景下灯现已随 agent 运行（黄）/停止（绿）/报错（红）实时变化。

### 桌面端：自定义音乐修复

4. 修复音符间隔（gap）上传到灯具后丢失：固件单音符只存频率与时长、没有间隔字段，之前间隔被完全丢弃，现在由桌面端把每个间隔编译成一个休止音符（频率 0）再上传，灯具上终于能听出间隔。末尾的间隔不再占用槽位空间。
5. 修复自定义音乐槽位互相覆盖：槽位不再直接取 ID 哈希值，改为从哈希槽向后探测第一个未被其他音乐占用的槽位，已分配的槽位在重新上传时保持不变；8 个槽位全部占满时给出明确报错。
6. 修复保存音乐即被当作「已安装」：只有在真正上传成功后才标记为已上传，配置页音乐列表显示分配到的槽位与「未上传」状态，保存后提示需先上传灯具才能播放，避免用户配置好了却在事件触发时静默无声。
7. 区分「未指定间隔」与「间隔为 0」：未指定时用默认 10 ms（音符不会粘连），显式设为 0 才是真正的连音；该区分在保存/重载往返后仍然保持。电脑试听与序列列表同步使用同一规则。
8. 增加单槽容量校验：编码后超过设备单槽 512 字节上限时直接报错，而不是上传到一半被固件判定为无效长度。
9. 删除与固件实际格式冲突的死代码 `music-codec.ts`（APM1 64 字节头 / 8 字节记录，与固件 4 字节记录不兼容），避免误导后续开发；音乐单元测试从 3 个扩充到 12 个，覆盖间隔编译、默认间隔、显式 0 连音、字节对齐、槽位容量与槽位避碰。

### 固件

10. 本版本**不升级 ESP32 固件**，安装包内置固件仍为 `0.1.24+25`（与 0.4.4 相同）。
11. 固件源码已移除自定义音乐播放时每个音符后写死的 `delay(10)`（该延迟会叠加在用户设定的间隔上使节奏偏慢），但该改动尚未提升 build 号、未编译分发。在内置固件上，间隔已能正常播放，仅每个音符多约 10 ms 的余量；待后续固件 build 号提升后即完全精确。存储格式、上传协议与 `PROTOCOL_VERSION` 均未改变，新旧固件数据互相可读。

## 0.4.4 - 2026-08-18

### 固件（ESP32-C3 `0.1.23-24`）

- 修复 USB 断开后灯未恢复蓝牙广播的问题：USB 拔出后正确调用 `resumeBleBroadcast()` 恢复广播与绿色呼吸。
- 新增 USB 心跳超时保护：连续 10 秒未收到 USB 心跳即进入蓝牙广播模式，避免桌面端异常时灯失联。
- USB OTA 升级成功后提示“设备升级成功，请重启设备”，并在重启前明确状态，避免误报超时。

### 桌面端

- 版本号提升到 `0.4.4`，Windows 安装器为 `AgentPulseSetup-0.4.4.exe`（本地自签证书 Authenticode 签名）。
- 实时事件面板（Dashboard 右侧）改为显示 `Agent · 项目 · 时间戳(持续秒数):状态 / 事件`，且结束态（green/red）的持续秒数冻结不再跳动，运行中（yellow）持续累加。
- 状态栏（statusline）显示时间戳（年月日时分秒）与任务累计秒数，格式为 `Agent · IDE · 时间(秒):状态 / 事件`。
- 更新清单 `update-manifest.json` 与 Ed25519 签名 `.sig` 同步到新固件与安装器；修正安装包大小与 SHA-256。

## 0.4.3 - 2026-08-17

AgentPulse v0.4.3 更新日志：

1. 新增 Cursor 支持：现在可为 Cursor 安装状态钩子，灯效跟随 Cursor 任务状态，配置页与安装器均提供 Cursor 入口。
2. 多 Agent 单独配置事件：支持为每个 Agent（Claude Code / Codex / CodeBuddy / WorkBuddy / Cursor）单独配置事件灯光，设备触发事件时严格走对应 Agent 的方案，不再回退全局默认。
3. 修复 USB 灯状态不同步：修复 USB 连接后灯不跟随任务状态（绿/黄/红）的问题，状态变更实时同步到设备，不再只在连接瞬间回灌一次。
4. 优化 USB 固件升级：优化 USB OTA 升级流程，修复升级后误报“有新固件/超时”、旧固件提交后正确判定完成，升级更顺畅稳定。
5. 固件：发布 ESP32 固件 0.1.22+23，继续向前兼容旧固件 0.1.21+22。

## 0.4.1 - 2026-08-14

### WorkBuddy、CodeBuddy 与多 Agent 展示

- 发布 WorkBuddy 与 CodeBuddy hooks 集成，状态可进入 AgentPulse Dashboard、悬浮窗和多设备 Agent 路由，与 Claude Code、Codex 一起显示。
- Windows 安装器和配置页提供 WorkBuddy / CodeBuddy hooks 安装入口，并保留用户已有 hooks 和设置。
- Ubuntu Collector 支持在 Ubuntu 服务器采集 Claude Code 状态，经 MQTT 同步到 Windows Dashboard，用于展示远端多 Agent、项目、状态和最后更新时间。

### 蓝牙与配置页

- macOS 连接后优先保留 BLE 广播中的 `AgentPulse-XXXX` 完整名称，避免 CoreBluetooth 缓存的通用名称覆盖设备后缀。
- 灯光方案管理对单灯用户保持可见，可以提前创建、编辑和预览方案；升级安装会关闭旧配置页进程，避免继续显示升级前的页面代码。

### 发布与固件

- 版本号提升到 `0.4.1`，Ubuntu Collector 运行包随根版本生成 `agentpulse-collector-0.4.1-linux-x86_64.tar.gz`。
- 本次不升级 ESP32 固件，发布包继续固定稳定固件 `0.1.21+22`，不发布本地 Wi-Fi/MQTT 测试固件 `0.1.22+23`。
- Dashboard 暂时屏蔽设备 Wi-Fi/MQTT 配网 UI，保留 MQTT 状态同步、Ubuntu Collector 和已配置 LAN 设备显示能力。
- TypeScript 类型检查、106 项 Node/Vitest 测试和 30 项 BLE Bridge 协议测试通过；2 项平台限定测试在 Windows 跳过。

## 0.4.0 - 2026-08-08

### 多设备与多 Agent

- 新增多盏 AgentPulse 灯同时连接与独立路由：每盏灯可跟随最新任务、指定项目或指定 Agent，支持 Claude Code 与 Codex 并行工作时分别显示状态。
- 单灯用户继续使用原有的“最新任务”行为；仅在检测到多盏灯时显示设备分配管理，避免增加不必要的配置复杂度。
- 支持为不同设备创建并分配独立灯光方案；配置页可选择目标设备或方案，灯效、亮度、事件映射和声音配置不再只能全局共用。
- Hook 状态同步补充 Agent 路由信息，即使同一项目在 Claude Code 与 Codex 之间切换，也能把事件发往正确的灯。

### 连接与设备管理

- 蓝牙和 USB 列表持续显示已连接、连接中及已保存的设备，并展示完整设备名称、连接状态、电量与充电信息。
- macOS 连接后优先保留 BLE 广播中的 `AgentPulse-XXXX` 完整名称，避免 CoreBluetooth 缓存的通用名称覆盖设备后缀；Windows 继续使用原有地址与配对名称。
- 扫描列表可直接连接第二盏蓝牙灯；断开附加设备会等待 Bridge 主动关闭 BLE 链路并回执，Windows 超时后终止完整进程树，避免 PyInstaller 子进程残留；该操作不会影响主灯或触发错误的自动重连。
- BLE Bridge 按逻辑设备启动独立实例，并通过 Windows mutex 或 Unix 文件锁阻止重复进程争抢同一设备。
- 多个免驱 USB 串口可分别连接和路由；主 USB/BLE 连接逻辑继续兼容旧配置。

### Dashboard 与悬浮窗

- 设备管理区新增跟随目标、灯光方案、识别、应用和断开操作，修复下拉菜单收起、应用后回退默认方案及设备名称截断等问题。
- 配置页始终显示灯光方案管理，单灯也可提前创建、编辑和预览方案；升级时 macOS 与 Windows 会关闭旧配置页进程，避免继续显示升级前的页面代码。
- 右上角蓝牙电量下移到对应设备列表，避免多设备时误以为电量属于其他灯；USB 主设备电量继续显示在顶部。
- 左上角灯明确标记为“最新任务状态”；悬浮窗同步显示最新任务所属的 `Codex`/`Claude` 和缩短后的项目名。
- “忘记设备”降为低频次要操作，避免与扫描、配对等常用操作抢占视觉层级。

### 构建与验证

- Windows 构建在打包悬浮窗前验证 Tcl/Tk，并在 PyInstaller 排除 `tkinter` 时直接失败，避免生成无法启动的悬浮窗。
- TypeScript 类型检查、77 项 Node/Vitest 测试和 27 项 BLE Bridge 协议测试通过。

## 0.3.2 - 2026-08-07

### 蓝牙连接与兼容

- 修复已绑定设备重新上电后虽然能够被扫描到，但不会自动重连的问题；已绑定设备再次出现时直接连接，不再重复应用首次绑定的 `-45 dBm` 近场门槛。
- “添加或更换配对”期间允许用户切回“近场自动连接”，并调整扫描按钮状态与两种蓝牙连接方式的提示。
- BLE Bridge 兼容旧固件被单条 BLE 通知长度截断的 `deviceInfo` 消息，解决 `0.1.19+20` 等旧固件读取设备信息超时、无法开始 OTA 的问题。
- 保持现有 OTA 命令和分区协议不变；历史发布固件 `0.1.8+9` 至 `0.1.20+21` 均包含当前升级所需的协议字段。

### 固件升级与电池保护

- ESP32 固件升级到 `0.1.21+22`，精简 `deviceInfo` 响应，确保升级所需信息可在单条 BLE 通知内完整返回。
- BLE 和 USB 固件升级前读取电池状态：未充电时电压必须高于 `3.60V`，充电状态下允许升级；电量不足时显示明确的充电提示。
- ESP32 同步执行升级电量校验，避免绕过 daemon 直接发送升级命令；电池电压持续一分钟不高于 `3.20V` 时自动断电保护。
- 升级传输中断、写入失败或镜像校验失败时不切换启动分区，设备继续运行原固件。
- 修复固件升级成功后页面仍提示存在新固件的问题，并将 BLE 操作取消与设备主动拒绝升级区分显示。

### 验证

- TypeScript 类型检查、60 项 Node/Vitest 测试和 20 项 BLE Bridge 协议测试通过，1 项仅限 macOS 的测试在 Windows 跳过。
- ESP32 `0.1.21+22` 编译通过，OTA 镜像大小为 `662,608` 字节，SHA-256 为 `136ea5304f087819034f51f0b0e50fba67296aaceb401c793fb5abe8e420b7c2`。

## 0.3.1 - 2026-08-07

### 系统蓝牙配对

- Dashboard 增加“自动连接 / 系统配对”切换，Windows 配对模式会打开系统蓝牙设置。
- BLE Bridge 通过 WinRT 读取系统已配对的 AgentPulse，只有完成系统配对的设备才能在配对模式中保存使用。
- Windows 新增配对完成后自动保存所选设备并立即连接；存在多个无法区分的历史配对时仍可手动选择。
- 蓝牙连接策略持久保存为“近场自动连接”或“已配对设备”；重新打开页面或重启 daemon 后保持用户选择。
- “添加或更换配对”与连接策略分离，已配对设备模式只连接 Windows 当前仍保持配对的已保存 MAC，不扫描或连接其他灯。
- ESP32 固件 `0.1.19+20` 启用可选 Just Works Bonding，近场自动模式无需系统配对；广播名称使用扫描列表中蓝牙 MAC 地址的最后两个字节作为设备尾号。
- ESP32 的可选 Bonding 不对普通近场连接强制认证，仅在 Windows 主动配对时启动认证，避免近场连接触发配对通知。
- ESP32 上电后两分钟允许新系统配对，配对完成后由 Windows 保存 Bond，后续 AgentPulse 自动连接。
- 扫描结果保留时间由 5 秒提高到 20 秒，改善部分蓝牙适配器设备列表闪烁的问题。

### Dashboard 与发布

- 有可用应用或固件更新时使用更醒目的琥珀色提示；没有更新时保持常规状态色。
- 实时事件面板固定在右侧当前视口内并独立滚动，最多保留 200 条事件；左侧页面继续整页滚动，事件增加不会撑高页面。
- Windows 安装包只携带固件发布清单引用的当前 `0.1.19+20` 镜像，不再混入历史或本地测试固件。
- Gitee 最新发布接口缺少附件大小时，daemon 改为读取该发布的附件详情后再校验，使应用与固件下载地址保持 Gitee 优先、GitHub 回退。
- Windows 系统配对连接目前仅支持 Windows；macOS 继续使用近场自动连接和手动绑定。

## 0.3.0 - 2026-08-06

### 更新源

- AgentPulse 应用和 ESP32 固件在线检查改为优先读取 Gitee Release 的已签名更新清单。
- Gitee 清单、签名或目标资产不可用/不匹配时，自动回退到 GitHub Release。
- 应用安装包和固件的下载地址统一按 Gitee、GitHub 排序；Gitee 下载失败后继续使用 GitHub，不需要用户手动切换。
- 两个来源继续使用同一份 Ed25519 签名清单、文件大小和 SHA-256 校验，回退不会降低更新验证要求。

### 设备与界面

- 正式整合 `0.2.18` 已完成的蓝牙近场绑定、USB 串口选择、设备调试和 Dashboard 响应式布局。
- 版本号统一提升到 `0.3.0`，包括 daemon、Dashboard、Windows 安装包和 BLE Bridge 协议版本信息。

### macOS 发布

- 正式发布 Intel `x86_64` 与 Apple Silicon `arm64` 两种架构的 Developer ID 签名、Apple 公证 PKG。
- macOS 同步获得蓝牙近场绑定、USB 串口选择、新版 Dashboard 和固件升级后配置重发。
- BLE Bridge 在 Python 3.9 下延迟求值类型标注，保持与 macOS 安装要求一致，不影响 Windows BLE 协议。
- Dashboard 将跨平台蓝牙 ID 统一显示为“设备标识”，正确覆盖 Windows MAC 地址与 macOS CoreBluetooth UUID。

## 0.2.18 - 2026-08-06

### 蓝牙连接

- 首次启动后持续扫描附近的 AgentPulse 设备，直到自动连接、手动绑定或用户停止扫描。
- Dashboard 实时列出设备名称、MAC 地址、RSSI 信号强度和采样次数，不再展示内部设备 ID。
- 近场自动绑定阈值调整为 `-45 dBm`；绑定结果写入本机配置，后续只自动连接已绑定设备，避免同一办公室内多台电脑串连其他灯。
- 支持手动绑定、切换和忘记蓝牙设备；忘记后恢复持续扫描和近场自动绑定。
- USB 串口连接时暂停蓝牙扫描与连接，USB 断开后自动恢复蓝牙流程。
- 蓝牙建立连接后不补发历史灯光事件，避免旧状态覆盖当前灯光。

### USB 串口

- Dashboard 新增独立的“USB 串口”设备列表，显示端口、设备名称、VID/PID、默认设备、已选择和已连接状态。
- 自动模式仅连接 AgentPulse 默认免驱串口 `303A:1001`，降低误连电脑上其他串口设备的风险。
- 支持用户手动选择并持久化指定串口；所选串口不存在时保持离线，不会静默切换到其他设备。
- USB 连接、断开和串口切换会同步刷新页面顶部连接状态；设备支持时继续显示电量和充电状态。
- Windows 使用 `COM*` 端口名；macOS 使用 `/dev/cu.usbmodem*` 或 `/dev/cu.usbserial*`，共享同一套枚举与选择逻辑。

### Dashboard

- 将“打开悬浮灯”和“配置”合并到灯状态面板，减少零散操作区。
- 蓝牙设备和 USB 串口改为两个独立且样式一致的设备列表。
- 调整设备调试、实时事件和左侧设备面板的对齐关系，实时事件区域自动填满右侧剩余空间。
- 优化桌面和窄屏响应式布局，避免设备标签、状态文字和操作按钮挤压或横向溢出。
- 页面打开时自动检查 AgentPulse/daemon 与 ESP32 固件在线版本，有更新时提示用户选择是否更新。

### 兼容与发布状态

- Windows 与 macOS 继续共享 daemon、Dashboard、配置和设备协议代码。
- Windows `0.2.18` 本地测试安装包已完成构建；本次文档更新时尚未上传 GitHub 或 Gitee。
- macOS 需要分别重新构建 `x86_64` 和 `arm64` 签名、公证 PKG；串口路径变化、蓝牙权限、睡眠恢复和覆盖升级仍需在真实 Mac 上回归。

### 验证

- TypeScript 类型检查通过。
- 52 项自动化测试通过，1 项仅限 macOS 的测试在 Windows 构建机跳过。
- Windows 生产构建和 Inno Setup 安装包构建通过。

## 0.2.17

- 完成 Windows/macOS 配置页 Hooks 安装适配，并保留第三方 hooks、备份和幂等写入行为。
- 正式提供 macOS `x86_64` 与 `arm64` 两种架构的签名、公证 PKG 和按架构在线更新。
- macOS 详细变更见 [MACOS_CHANGE_RECORD.md](MACOS_CHANGE_RECORD.md)。
